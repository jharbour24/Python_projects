# Core dependencies
pydantic>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
click>=8.1.0
pillow>=10.0.0

# Testing
pytest>=7.4.0

# Optional: for semantic search (falls back to TF-IDF if not installed)
# sentence-transformers>=2.2.0

# Optional: for better charts in eval
# matplotlib>=3.7.0
